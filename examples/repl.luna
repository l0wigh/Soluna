; Simpliest form of REPL for Soluna
; Look at Oracle.luna for a more advanced form of REPL
(function repl () (do
	(defvar kill-switch true)
	(while kill-switch (do
		(defvar user-input (input "> "))
		(writeln (eval user-input))
	))
))
(repl)
