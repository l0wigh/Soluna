; PGCD program to prove the turing-completness of Soluna
(defvar pgcd (lambda (a b) (if (= b 0) a (pgcd b (mod a b)))))

(write "PGCD(27, 18) = ")
(writeln (pgcd 27 18))

(write "PGCD(100, 30) = ")
(writeln (pgcd 100 30))
